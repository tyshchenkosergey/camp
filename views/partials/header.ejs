<!DOCTYPE html>
<html>
	<head>
		<title>Camp?</title>
		<link rel="stylesheet"
			  href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
		<link rel="stylesheet" type="text/css" href="/stylesheets/app.css" />
	</head>
	<body>
		
		<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<div class="navbar-header">
			<a class="navbar-brand " href="/"> <span class="text-muted">Camp?</span></a>
			<a class="navbar-item " href="/location"> <span class="text-muted">Locations</span></a>
		</div>
		<div>
			<ul class="nav justify-content-end">
				<% if(!currentUser){ %>
					<li class="nav-item">
						<a class="nav-link text-muted" href="/login">Log in</a>
					</li>
					<li class="nav-item">
						<a class="nav-link text-muted" href="/register">Sign In</a>
					</li>	
				<% }else{ %>
					<li class="nav-item">
						<a class="nav-link text-muted" href="#">Welcome, <%=currentUser.username%></a>
					</li>
					<li class="nav-item">
						<a class="nav-link text-muted" href="/logout">Log Out</a>
					</li>
				<%	} %>
			</ul>
		</div>
	</div>
</nav>
		<div class="container">
			<% if(error && error.length>0){ %>
			<div class="alert alert-danger" role="alert">
				<h3><%= error %></h3>
			</div>
			<% } %>
			<% if(success && success.length>0){ %>
			<div class="alert alert-success" role="alert">
				<h3><%= success %></h3>
			</div>
			<% } %>
		</div>
		